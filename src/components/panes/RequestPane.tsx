import { FC } from 'react';
import { useParams } from 'react-router-dom';

import request from '../../services/request';
// import Http from './../../components/http'
const RequestPane: FC<any> = ({ id, updateCol }) => {
  const nodeType = 1;
  return <div>1234</div>;
};

export default RequestPane;
