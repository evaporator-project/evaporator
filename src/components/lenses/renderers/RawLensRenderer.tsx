import { FC } from 'react';

import { HoppRESTResponse } from '../../../helpers/types/HoppRESTResponse';

const RawLensRenderer: FC<{ response: HoppRESTResponse }> = ({ response }) => {
  return <div>RawLensRenderer</div>;
};

export default RawLensRenderer;
